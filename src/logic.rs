use poker::{Hand, HandValue};

// Returns the probability that the handvalue exists given your hand and the 
// total amount of cards
fn handvalue_prob(hv: &HandValue, hand: &Hand, total: u8) -> f64 {
    unimplemented!()
}
